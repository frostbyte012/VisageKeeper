# Figures

Drop paper figures here and un-comment the matching `![...]()` block in the
root `README.md`.

Suggested names (referenced as TODO placeholders in the README):

| file | shows |
|---|---|
| `overview.png`     | system overview: noise -> denoise -> vote -> certificate |
| `attenuation.png`  | the attenuation law, E[c] vs sigma |
| `accelerator.png`  | FPGA accelerator architecture (ZCU104) |
| `table4.png`       | optional rendered results table |

PNG or SVG. Keep each under ~1 MB so clones stay fast.
